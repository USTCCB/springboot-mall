<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ustccb.mall.mapper.OrderMapper">
    <resultMap id="BaseMap" type="com.ustccb.mall.entity.MallOrder">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="goodsId" column="goods_id"/>
        <result property="quantity" column="quantity"/>
        <result property="amount" column="amount"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>
    <insert id="insert" parameterType="com.ustccb.mall.entity.MallOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mall_order(user_id, goods_id, quantity, amount, status) VALUES(#{userId}, #{goodsId}, #{quantity}, #{amount}, #{status})
    </insert>
    <select id="findById" resultMap="BaseMap">SELECT * FROM mall_order WHERE id = #{id}</select>
    <update id="updateStatus">UPDATE mall_order SET status = #{status} WHERE id = #{id}</update>
</mapper>
